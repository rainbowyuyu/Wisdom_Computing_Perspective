# 2026-09-19 调用链路加固记录

本次检查范围为收费模型入口、渲染入口、凭据、会话、请求体和前端依赖。不是完整渗透测试。

| 原问题 | 风险 | 已处理 | 文件 |
| --- | --- | --- | --- |
| `.env` 被 Git 跟踪且含真实凭据格式 | 严重 | 从索引取消跟踪，本地文件保留，增加忽略规则；历史密钥仍需拥有者轮换 | `.gitignore` |
| 自定义 Python 渲染未鉴权，关键字黑名单不能阻止任意执行 | 严重 | 执行入口限主管理员或服务端 WISDOM_CODE_RUNNERS 白名单；普通解题使用固定场景 | `app/request_guard.py` |
| 旧矩阵渲染将操作类型插入 Python 字符串 | 严重 | 固定操作白名单、任务 ID 检查、数值/矩阵大小限制，非法数字不再静默变为零 | `logic/manim_generator.py` |
| 多个入口可无限调用模型，重试/保存标签隐性消费 | 高 | 统一受控客户端；登录、持久日额度、并发、输出和输入上限，关闭 SDK 重试，默认本地标签 | `app/usage_guard.py`, `app/config.py` |
| Cookie 过期后服务端会话仍有效 | 中 | 服务端 24 小时过期；验证码五分钟及单次尝试；HTTPS 下 Secure Cookie | `app/store.py`, `app/routers/auth.py` |
| 上传/上下文长度无边界，超长任务影响可用性 | 中 | 实测请求体上限（含无 Content-Length）、上传超时、图片 5 MB、模型上下文和输出限额 | `app/request_guard.py`, `app/models.py` |
| 跨站提交缺少统一检查、转发 IP 可混淆计数 | 中 | Origin 检查、可信代理精确列表及覆盖 X-Real-IP；关闭 Uvicorn 自动代理解析 | `main.py`, `app/request_guard.py` |
| 同步模型/数据库工作影响事件循环 | 中 | 智能体在线程处理；OCR 线程保留额度身份；算式保存和登录/注册移到线程路由 | `app/routers/detect.py`, `app/routers/formulas.py`, `app/routers/auth.py` |
| 前端依赖 nanoid/postcss 存在已知高危公告 | 高 | 更新兼容的锁定版本，`npm audit fix` 后审计 0 项，构建通过 | `frontend/package-lock.json` |
| 同一题并发重复提交，参数错误也先扣功能次数 | 高 | 对解题、智能体、代码助手及动画 JSON 请求先校验，再按账户/IP、接口和规范化参数拦截正在处理的重复请求；重复请求不再次扣功能次数，完成后可手动重试 | `app/request_guard.py`, `app/usage_guard.py` |
| 模型鉴权、限流或服务故障时继续反复请求 | 高 | 按服务地址及密钥的哈希持久化故障状态；401/403 暂停 300 秒、429 暂停 60 秒，120 秒内连续 3 次连接/超时/5xx 故障暂停 30 秒；期间不新增模型调用 | `app/usage_guard.py` |
| 用户停止请求后，排队线程仍可能开始调用模型 | 高 | 排队与调用前检查请求取消状态；独立后台任务保留自己的生命周期；异步模型请求增加 70 秒总等待边界 | `app/usage_guard.py`, `app/math_jobs.py` |
| 旧动画绕开共享队列，自动修复重复消费，取消清理可能提前归还名额 | 高 | 矩阵、开发者及旧流式动画共用渲染名额；等待最多 60 秒；可修复错误最多请求模型诊断与修复一次，环境/资源/权限错误不自动修复；失败保留解答与代码；清理抵御重复取消 | `app/routers/detect.py`, `app/routers/devtools.py`, `app/async_cleanup.py`, `app/render_repair.py` |

后续补充：数学分步动画只允许替换诊断定位的单步 LaTeX，执行白名单命令检查，并比较数字、变量和关系符号，不能引入 Python；原始输入不原地修改。此检查不是任意数学表达式的等价性证明。开发者修复脚本经过原有检查且仅在原管理员权限内执行，不能视为进程沙箱。模型输出截断、拒绝修复、无变化或格式无效时直接停止，不追加付费修复请求。诊断文本截短并去除常见凭据格式，修复等待支持取消且不占用渲染名额。

自动修复验证：新增 17 项测试覆盖修复成功、二次失败、环境错误、额度拒绝、取消、危险脚本拦截及预览/视频共享次数；相关后端回归累计 125 项通过，13 项 JavaScript 测试、两套前端的 4 项浏览器检查和 Vue 构建通过。使用模拟模型回复，分别让真实 Manim 渲染缺花括号的公式及拼错对象名称的脚本，两条链路均在首次失败后修复并生成 MP4；未用真实付费模型测试判断质量。网页已通过 `main.py` 重启加载，并验证首页及动画能力接口返回 200。

上述去重针对正在处理的同一路径 JSON 请求，不是长期结果缓存，OCR multipart 上传仍使用次数、频率和体积限制。已发给模型的请求可能已经计费，取消不会承诺退还服务商账单；同步 SDK 使用 70 秒网络超时，不宣称它是完整调用的硬性总时长。故障状态只存哈希、次数和时间，不存密钥或服务商原始错误内容。

本轮验证：136 项后端回归通过（含真实 MySQL 的独立账户测试）；静态与 Vue 入口共 4 项浏览器检查通过，覆盖超时恢复、取消、重试和额度提示。模型异常使用模拟服务注入，不消耗真实模型 token。实际矩阵 Manim 渲染成功生成 MP4；同时修复旧矩阵脚本中格式化字符串的语法错误。运行中的网站已验证首页返回 200、非法解题参数返回 422、`.env` 和视频目录 Python 源码请求返回 404。安全模式检索未在本轮检查的应用与前端源文件中发现匹配的明文服务密钥，但这不等于完成历史仓库或所有格式的凭据扫描。

限制：管理员 Python 权限不是沙箱；会话依然为单进程内存存储，重启需重新登录。Python 依赖未执行漏洞库扫描。现有第三方 CDN 脚本使严格 CSP 尚需单独兼容评估，此次增加 nosniff、Referrer-Policy、SAMEORIGIN，不宣称覆盖所有 XSS/SQL 风险。模型日 token 指标为保守预留，不能替代服务商账单或证明任意数学题正确。
