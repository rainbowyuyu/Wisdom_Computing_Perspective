# 网站目录结构

网站所需源码、资源、数据库 SQL 和部署配置均位于 `html_root`。

目录说明见 [README.md](README.md)，宝塔操作见 [deploy/BAOTA.md](deploy/BAOTA.md)。

原 `vue_root` 已移入 `frontend/`；`visdom_db.sql` 是 MySQL 安装与升级脚本，不是 SQLite 数据文件。
